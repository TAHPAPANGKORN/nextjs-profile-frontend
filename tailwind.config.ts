import type { Config } from "tailwindcss";

const config: Config = {
    theme: {
        screens: {
            sm: "375px",
            md: "768px",
            lg: "1200px",
        },
    },
};

export default config;
